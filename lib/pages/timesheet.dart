import 'package:flutter/material.dart';

class TimesheetPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Add new timesheet"),
        ),
        body: Center(
          child: Text("Selamat Datang Timesheet Page"),
        ));
  }
}
